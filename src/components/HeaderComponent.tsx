import React from 'react';
import '@/css/header.css'
const HeaderComponent = () => {
    return (
        <div className={'header-container'}>
            <nav>
                <a className={'a-link'} href="">Header</a>
                <a className={'a-link'} href="">sadas</a>
                <a className={'a-link'} href="">cvdfvdf</a>
                <a className={'a-link'} href="">fiokvd</a>
            </nav>
        </div>
    );
};

export default HeaderComponent;